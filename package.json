{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build -- --censor-lib --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "pulp": "^16.0.1",
    "purescript": "^0.15.2",
    "purescript-psa": "^0.8.2",
    "rimraf": "^3.0.2"
  }
}
