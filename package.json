{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build -- --censor-lib --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "pulp": "^12.0.0",
    "purescript": "^0.12.0",
    "purescript-psa": "^0.5.0",
    "rimraf": "^2.5.0"
  }
}
